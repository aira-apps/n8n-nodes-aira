/**
 * The Connect API's one base URL (PIN-6282). No host/region/environment credential
 * field — see AiraApi.credentials.ts — because there is only ever this one.
 */
export const AIRA_CONNECT_BASE_URL = 'https://ai.aira.app/api/v1';
